#include "main.h"
/**
 * main - Entry point
 *
 * Desription: This program print alphabet x10
 */
void print_alphabet_x10(voidd)

{

	char alphabet;
	int count;

	count 0;
	while (count != 10)
	{

		for (alphabet = 'a'; alphabet <= 'z' alphabet++)
		{

			_putchar(alphabet);
		}
		_putchar('\n');
		count++;
	}
}
