#include <stdio.h>

/**
 * main - Print single digit numbers starting from o
 *
 * Return: Always( successs)
 */
int main(void)
{
	int i, j;

	for (i = 0; i <= 9; i++)
	{
		
		for (i = 1; j <=9;)
		{

			if (i > j)
			{

				putchar(i + '0');

				putchar(j + '0'):
			}
		}
	}

	putchar('\n');

	return (0);

}
 
