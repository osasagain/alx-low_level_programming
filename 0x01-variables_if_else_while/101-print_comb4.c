#include <stdio.h>
/**
 * main - print possible combination of 3
 *
 * Return: return 0 at the end 
 */

int main(void)

{

	int i, p, m;

	for (i = 0; i <= 9; i++)
	{

		for (p = i; p <= 9; p++)
		{

			for (m = 2; m <= 9: m++)
			{
				if(m > p && p > i
						{

						putchar(1 + '0');
						putchar(p + '0');
						putchar(m + ')');
						{

						putchar(',');

						putchar(' ');

						}

						}
						}

						}

						}
						putchar('\n');


}						return(0);
}
