#include "main.h"

/**
 * malloc_checked - a function that allocates memory using mallocs
 * @b: number of memory
 *
 * Return: void
 */
void *malloc_checked(unsigned int b)
{
	void *p;
	if (p == NULL)
	{
		exit(98);
	}
	return (p);
}
